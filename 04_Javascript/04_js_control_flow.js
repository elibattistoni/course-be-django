//alert("hello")

var hot = false
// var temp = 100
var temp = 100

if (temp >= 50 && temp <= 80) {
    // console.log("temp is greater than 80");
    // hot = true // this reassigns the variable
    console.log("temp between 50 and 80 degrees");
} else if (temp > 80 && temp < 100) {
    console.log("temp between 80 and 100 degrees");
} else {
    console.log("temp lower than 50 or temp greater or equal than 100")
}

// console.log(hot);